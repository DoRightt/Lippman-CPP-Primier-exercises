#include <iostream>
#include <vector>

using std::string;
using std::cout;
using std::cin;
using std::endl;
using std::vector;

int main() {
    int arr[10];

    for (size_t i = 0; i != 10; ++i) {
        arr[i] = i;
    }

    return 0;
}